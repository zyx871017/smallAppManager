import React from 'react';
import {RaisedButton, TextField} from 'material-ui';
import {styles} from './LoginStyle';

class Login extends React.Component {
    render() {
        return (
            <div>
                <div style={styles.title}>Login page.</div>
            </div>
        )
    }
}

export default Login;